import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

@RunWith(JUnit4.class)
public class RpcServerTest {
    @Test
    public void start_server_test() {
//        Iterable<MedusaRpcFactory> servicesRpc = ServiceLoader.load(MedusaRpcFactory.class);
//        for(MedusaRpcFactory serviceRpc: servicesRpc) {
//            serviceRpc.start(5060);
//            serviceRpc.blockUntilShutDown();
//        }
    }
}
